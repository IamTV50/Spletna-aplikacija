import React, { useState } from 'react';

function Log() {

    const Log = () => {
        const [log, setLog] = React.useState([]);

        try {

        } catch (error) {
            console.error(error);
        }
    }

    return (

    );
}


export default Log;